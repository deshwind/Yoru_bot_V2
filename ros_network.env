# Shared ROS 2 network settings - source this on BOTH machines
# (start_server.sh and start_pi.sh do it automatically).
#
# On a home Wi-Fi router nothing else is needed: both machines use the same
# domain ID and find each other automatically (DDS multicast discovery).

export ROS_DOMAIN_ID=42
export RMW_IMPLEMENTATION=rmw_fastrtps_cpp

# --- University / corporate Wi-Fi (multicast blocked) ------------------
# Campus Wi-Fi blocks multicast, so both machines discover each other
# through a FastDDS discovery server hosted ON THE ROBOT (the Pi), so the
# robot never depends on the laptop being up. start_robot.sh starts the
# server automatically. If the Pi's IP changes, update it here on BOTH
# machines (Pi IP shown by: hostname -I).
export ROS_DISCOVERY_SERVER=172.18.16.34:11811
export ROS_SUPER_CLIENT=true
